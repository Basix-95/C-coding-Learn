#include <stdio.h>

int main() {
    int no = 10;
    float dec = 10.0;
    char name[] = "Ten";
    long integer = 100000000000L;
    double bdec = 10.101010101010010101;

    printf("%d, %f, %s, %ld, %lf\n", no, dec, name, integer, bdec);

    return 0;
}

